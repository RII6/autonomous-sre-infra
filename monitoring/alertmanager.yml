route:
  group_by: ['alertname']
  group_wait: 2s
  group_interval: 10s
  repeat_interval: 1m
  receiver: 'python-healer'

receivers:
  - name: 'python-healer'
    webhook_configs:
      - url: 'http://app:8000/api/webhook/alert'
        send_resolved: true
